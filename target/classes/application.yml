management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true

server:
  port: 8080

spring:
  application:
    name: kafrika-backend
